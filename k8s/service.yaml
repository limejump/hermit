---
apiVersion: v1
kind: Service
metadata:
  name: hermit
  namespace: cx
spec:
  selector:
    app: hermit
  ports:
  - port: 80
    targetPort: web
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: hermit
  namespace: cx
spec:
  hosts:
  - hermit.dev.limejump.io
  gateways:
  - hermit-gateway
  http:
  - match:
    - headers:
        x-limejump-magic-header:
          exact: yeet
    route:
    - destination:
        host: hermit
        subset: feature-x
  - route:
    - destination:
        host: hermit
        subset: master
---
apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: hermit
  namespace: cx
spec:
  host: hermit
  subsets:
  - name: master
    labels:
      version: master
  - name: feature-x
    labels:
      version: feature-x
