apiVersion: apps/v1
kind: Deployment
metadata:
  name: hermit-feature-x
  namespace: cx
spec:
  replicas: 3
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: hermit
      branch: feature-x
  template:
    metadata:
      labels:
        app: hermit
        branch: feature-x
    spec:
      containers:
      - name: hermit
        image: eu.gcr.io/limejump/hermit:b2a73a3b325b397956e0be831a1667edb9f46df3
        envFrom:
        - secretRef:
            name: keys
            optional: false
        resources:
          limits:
            memory: 128Mi
            cpu: 100m
          requests:
            memory: 128Mi
            cpu: 10m
        ports:
        - name: web
          containerPort: 3000
        readinessProbe:
          httpGet:
            port: web
            path: /v1/ready
